# compiled binary (built by `make build`; carries file caps on the inode)
/porthawk

# go
*.test
*.out
/dist/

# Dumped from the running kernel by `make bpf`, 3 MB of generated types.
# Only needed to recompile bpf/udp.c; the compiled object is committed, so
# a plain `go build` never wants this file.
/bpf/vmlinux.h

# pre-edit copies of the instruction files, kept locally
*.original.md

# editor / os
*.swp
*~
.DS_Store
.idea/
.vscode/
